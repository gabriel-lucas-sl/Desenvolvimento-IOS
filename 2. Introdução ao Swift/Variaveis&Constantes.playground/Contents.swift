// Criando uma variável com tipo explícito
/* Podemos associair swift com typescript, quando falamos em criar variáveis com tipo explícito, pois ambas possuem o mesmo formato de declaração */
var name: String = "Steve Jobs"

// Criando uma variável utilizando inferência de tipo
var nameTwo = "É poder, aceita porque doi menos"

// Quando usamos o comando print, podemos separar o conteúdo do que será impresso com vírgula
print(name, "|", nameTwo)

// Criando 3 variáveis em uma única linha
/* Isso também é próximo do que ocorre no java */
var x = 12, y = "Oi", z = 3.3

// Criando constantes
let pi = 3.141592
let earthGravityt = 9.81
//earthGravityt = 10


